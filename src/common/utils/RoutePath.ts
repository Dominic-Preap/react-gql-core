export const RoutePath = {
  login: '/login',
  dashboard: '/dashboard',
  products: '/products',
  users: '/users',
  notFound: '/404',

  // ---------------------------------------------
  // tslint:disable-next-line:max-line-length
  // x: (id: string) => RoutePath.x.replace(':id', id)
};
